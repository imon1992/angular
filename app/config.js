myApp.config(function($stateProvider) {
    // An array of state definitions
    var states = [

      { 
        name: 'people', 
        url: '/people', 
        component: 'people'
      }
    ]
    
  });